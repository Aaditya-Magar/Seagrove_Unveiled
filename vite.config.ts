// The project Vite config is based on the shared TanStack Start toolkit.
// Do not duplicate plugin configuration here, or the build may include
// duplicate plugins for React, Tailwind, TS path aliases, and environment handling.
import { defineConfig } from "@lovable.dev/vite-tanstack-config";

export default defineConfig();
